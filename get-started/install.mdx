---
title: "Install"
description: "Install Sealos Skills in Codex, Claude Code, skills.sh, or other supported AI tools."
keywords: ["install", "codex", "claude code", "skills.sh", "plugin"]
---

{/* TODO: Sync install commands from sealos-skills distribution/platforms.json. */}

<Tabs>
  <Tab title="Codex">
    {/* TODO: marketplace add + plugin add + $sealos usage */}
  </Tab>
  <Tab title="Claude Code">
    {/* TODO: marketplace add + plugin install + /sealos usage */}
  </Tab>
  <Tab title="skills.sh">
    {/* TODO: npx skills add + direct skill entries */}
  </Tab>
  <Tab title="Other hosts">
    {/* TODO: Gemini, Qwen, OpenClaw, CodeBuddy — with claim levels from platforms.json */}
  </Tab>
</Tabs>

<Info>
Plugin users invoke `$sealos` (Codex) or `/sealos` (Claude Code). Direct `skills.sh` users invoke `/sealos-deploy`, `/sealos-database`, or `/sealos-s3`.
</Info>
