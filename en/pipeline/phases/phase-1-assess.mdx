---
title: "Phase 1 — Assess"
description: "Judge deployability, choose the path, and write official_template."
keywords: ["phase 1", "assess", "analysis.json", "official template"]
---

## Purpose

Phase 1 judges whether the project can run as a cloud service. It chooses the fast path or the standard path. It writes `official_template`. On an exact catalog match, it **fetches the official template verbatim** to `.sealos/template/index.yaml`.

UPDATE mode skips this phase.

Do not score the project. Do not select images. Do not run [Phase 4](/en/pipeline/phases/phase-4-template) template generation (conversion, digest pinning, deploy gate). Do not create cloud resources. Do not fetch or rewrite YAML except the official template verbatim fetch below.

## Blocklist

**STOP** only when both rows are **100% confirmed**. Otherwise **CONTINUE**. Do not **STOP** on uncertainty or weak evidence.

| # | Condition |
| --- | --- |
| 1 | The project cannot run as a cloud service |
| 2 | No part of the repository can deploy as a cloud service |

The repository is the source boundary. This check writes no artifact. Do not prompt the user.

## Inputs

| Input | Source | Required |
| --- | --- | --- |
| `analysis.json` | [Phase 0](/en/pipeline/phases/phase-0-preflight) | Yes |
| Official template catalog | [`kb-0.9/template`](https://github.com/labring-actions/templates/tree/kb-0.9/template) | Yes |

Phase 0 must have written `runtime_profile`, `work_dir`, and `repo_name`. `github_url` may be `null`.

## Outputs

| Output | Path |
| --- | --- |
| `official_template` | `.sealos/analysis.json` |
| Official template (on exact match) | `.sealos/template/index.yaml` |

```json
{
  "official_template": "https://raw.githubusercontent.com/labring-actions/templates/{commit}/template/{name}/index.yaml"
}
```

One exact match → URL string. Otherwise → `null`. This is independent of the fast path.

[Phase 2](/en/pipeline/phases/phase-2-discover) writes the `deployment_plan` pointer.

## Preconditions

- [Phase 0](/en/pipeline/phases/phase-0-preflight) passed
- [Mode detection](/en/pipeline/mode-detection) is **DEPLOY**

## Phase constraints

Read when entering. Obey **while executing**.

| ID | Constraint | Notes |
| --- | --- | --- |
| P1-01 | Do not score or choose images | Assess path, not build |
| P1-02 | Do not run Phase 4-style template generation | No conversion, digest pin, or deploy gate |
| P1-03 | Do not create cloud resources | Except verbatim official template fetch, do not fetch or rewrite other YAML |
| P1-04 | Official templates: exact match only | No fuzzy match by name, language, or framework |
| P1-05 | Blacklist STOP only at 100% certainty | Insufficient evidence → CONTINUE |
| P1-06 | When `official_template` is non-null, fetch verbatim | Do not rewrite, merge, or regenerate from the repo |

## Phase validation

Run **after outputs are written**:

```bash
node skills/sealos-deploy/scripts/validate-phase-1.mjs --dir "$WORK_DIR"
```

The script checks:

| ID | Check |
| --- | --- |
| P1-V01 | `official_template` is `null` or a labring-actions raw URL |
| P1-V02 | Phase 0 four fields preserved |
| P1-V03 | When `official_template` is non-null, `.sealos/template/index.yaml` exists |

On failure, do not **CONTINUE → Phase 2** or **CONTINUE → Phase 5**.

## Procedure

### 1. Blocklist check

See the blocklist above. Continue when it passes.

### 2. Read `analysis.json`

Keep `runtime_profile`, `work_dir`, `repo_name`, and `github_url`. Do not modify them.

### 3. Official template exact match

Match against [labring-actions/templates `kb-0.9/template`](https://github.com/labring-actions/templates/tree/kb-0.9/template).

An exact match means the template declares the same source project. Do not fuzzy-match by name, language, or framework. Do not similarity-match.

| Match result | `official_template` |
| --- | --- |
| No match or multiple matches | `null` |
| Catalog unreachable | `null` (do not **STOP**) |
| One exact match | Raw URL of that template `index.yaml` |

When there is one exact match, write the URL whether or not the run later takes the fast path.

**Fast path** (skip Phases 2–4) requires all of:

| Condition | Requirement |
| --- | --- |
| `official_template` | Non-null |
| `runtime_profile` | `sandbox`, or `local` with no uncommitted changes (exclude `.sealos/`) |

`sandbox` does not check uncommitted changes.

On `local` with uncommitted changes, `official_template` may still be non-null. The run takes the standard path.

### 4. Write `analysis.json`

Write `official_template`. Keep the four Phase 0 fields. Full [schema](/en/specs/artifact-schemas) validation is not required in this phase.

### 5. Fetch official template

Run only when `official_template` is non-null:

1. Fetch `index.yaml` from the `official_template` URL.
2. Write it **verbatim** to `.sealos/template/index.yaml`. Do not rewrite, merge, or regenerate from the current repo.
3. Fetch failure (URL unreachable, empty body) → **STOP**.

On the standard path, a later Phase 4 run may **overwrite** this file. The fast path uses this file in Phase 5.

## Stop conditions

| Result | Condition |
| --- | --- |
| **STOP** | Both blocklist rows are 100% confirmed |
| **STOP** | `official_template` is non-null but the official template fetch failed |
| **CONTINUE → Phase 5** | `official_template` is non-null and fast-path conditions are met |
| **CONTINUE → Phase 2** | All other cases |

## Acceptance

- `official_template` matches the match result
- Phase 0 four fields are unchanged
- When `official_template` is non-null, `.sealos/template/index.yaml` exists as an unmodified copy of the fetched official YAML

## Schema links

[Artifact schemas](/en/specs/artifact-schemas)

## Implementation map

`skills/sealos-deploy/modules/phase-1.md`
