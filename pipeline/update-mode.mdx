---
title: "Update mode"
description: "In-place image rollout when an existing deployment is detected."
keywords: ["update", "rollout", "rollback"]
---

## When UPDATE mode runs

{/* TODO: state.json last_deploy + live kubectl verification. */}

## UPDATE workflow

{/* TODO: Build & Push → resolve digest → kubectl set image → verify rollout. */}

## Auto-rollback

{/* TODO: Rollback behavior on failed rollout verification. */}

## Skipped phases

{/* TODO: UPDATE skips assess, template, and deploy phases. */}
